# Branch `experiment/monetization`

Percobaan **monetisasi** (paket Free/Pro, batas unit, export CSV/PDF Pro-only, kode promo). Bukan rilis stabil.

- **Raw / stabil:** `main` — preview [http://localhost:61036/](http://localhost:61036/) dengan `serve.bat`.
- **Eksperimen:** branch ini — preview [http://localhost:61037/](http://localhost:61037/) dengan `serve-experiment.bat` (variabel `PORT=61037`).

Gabung ke `main` hanya setelah keputusan produk & QA.
